F p1 && F p2 && F p3

&&

G (
    (false U[1, infty) true)

    &&

    ((p1 && !(p2 || p3)) || (p2 && !(p1 || p3)) || (p3 && !(p1 || p2)))

    &&
    
    ((p1 -> F[0, 3] p1) && (p2 -> F[0, 4] p2) && (p3 -> F[0, 5] p3))

  )
